# Handoff: same_sampled_spring_unified port (paused 2026-07-02, evening)

Resume point for porting the PyTorch `same_sampled_spring_unified` optimizer into
`vmcnet` (JAX). Read this, the spec, the plan, and the SDD ledger to pick up exactly
where we left off.

## TL;DR — can you just close the laptop?

**Yes.** Everything needed to resume is durable:
- All commits are pushed to your fork `origin` (`jefferyren/vmcnet`), branch
  `same-sampled-spring-unified`.
- The spec, plan, and this handoff are committed (so they survive on the fork even if
  the laptop is wiped).
- The SDD progress ledger and task briefs/reports live in `.superpowers/sdd/`
  (git-ignored, local disk only — fine as long as the laptop is intact; the committed
  handoff + ledger contents below are the backup).

To resume: reopen this session if it's still around, **or** start a fresh Claude session
and say "resume the same_sampled_spring_unified port from the handoff." I will re-read
the plan + ledger and continue.

## Where we are

Process: **subagent-driven development** executing
`docs/superpowers/plans/2026-07-02-same-sampled-spring-unified.md`
(spec: `docs/superpowers/specs/2026-07-02-same-sampled-spring-unified-design.md`).

- **Task 0 (env):** DONE. Interpreter is the existing conda env
  `/Users/jefferyren/anaconda3/envs/vmcnet-py312/bin/python` (Python 3.12; the repo pins
  `jax==0.4.34` which needs ≥3.10, so the README's "3.9" is stale). Baseline tests green.
- **Task 1 (state pytree + `x_star` helper):** IMPLEMENTED and committed
  (`fc2812d`), pushed. Tests 2/2 pass. **Not yet through its SDD task-review.**
- **Tasks 2–8 + final review:** not started.

Branch commits so far:
```
fc2812d Add same_sampled_spring_unified state pytree and x_star initializer   <- Task 1
bc12f97 Point plan at vmcnet-py312 conda env (repo needs Python >=3.10)
cf5373b Add implementation plan for same_sampled_spring_unified
9df524c Refine spec: probe_lr defaults to base learning_rate via sentinel
d1c26c1 Add design spec for same_sampled_spring_unified optimizer
```

## Immediate next action (tomorrow)

Run the SDD task-review for Task 1 (base `bc12f97`, head `fc2812d`), then fix its two
known concerns via a fix subagent, re-review, and mark Task 1 complete. Then proceed
through Tasks 2–8 and the final whole-branch review.

## Two open concerns from Task 1 (must fix in review) — and they are STANDING

1. **mypy:** the `SameSampledSPRINGUnifiedState` NamedTuple annotates pytree fields
   (`phi`, `z_probe`, `phi_probe`, `x_star`) with the TypeVar `P`, which mypy reports as
   unbound (4 errors). Fix: type those fields as `PyTree` (from `vmcnet.utils.typing`),
   or make the NamedTuple `Generic[P]` following
   `vmcnet/utils/checkpoint.py:RunningEnergyVariance`.
2. **flake8 `--select D`:** the new test functions lack docstrings (D103). Existing repo
   tests carry one-line docstrings, and `lint.sh` runs the D-checks on `tests/` too. Add
   a one-line docstring to **every** test function. **This applies to all test functions
   added in Tasks 2–7 as well** — fold it into each task's dispatch so reviewers don't
   re-flag it every time.

(Neither breaks the tests, which pass; both are caught by `lint.sh`/CI. Task 8 is the
lint/type sweep, but fixing #1/#2 as we go keeps each task clean.)

## Key facts for whoever resumes

- **Interpreter / commands:** `/Users/jefferyren/anaconda3/envs/vmcnet-py312/bin/python -m pytest|black|flake8|mypy ...`
- **Git:** branch `same-sampled-spring-unified`; push to `origin` (your fork) only,
  never `upstream` (`jeffminlin/vmcnet`). **No `Co-Authored-By` trailer** in commits.
- **SDD artifacts:** ledger `.superpowers/sdd/progress.md`; briefs/reports
  `.superpowers/sdd/task-N-{brief,report}.md`; diff packages generated by the skill's
  `scripts/review-package BASE HEAD`.
- **Model plan:** implementers = sonnet; task reviewers = sonnet; final whole-branch
  review = opus.
- **Correctness anchor (Task 4):** `test_main_update_matches_base_spring` — with
  `adaptive_eta` off and β pinned at `mu`, the main-path `phi` update must equal
  `vmcnet/updates/spring.py:get_spring_step`'s output. If that ever fails, the operator
  mapping is wrong — debug there first.

## Remaining tasks (from the plan)

- Task 2: `_build_operators` (shared `apply_A`/`apply_Aᵀ` + one `eigh(T)`) + operator
  sanity tests (mean-zero, adjoint, T reconstruction).
- Task 3: `_adaptive_beta_update` (sliding buffer + β schedule) + numpy-reference tests.
- Task 4: `get_same_sampled_spring_unified_step` (main + probe + adaptive) + equivalence
  to base SPRING.
- Task 5: `constrain_norm`, update-param-fn constructor, `initialize_...` + jitted
  single-device apply test.
- Task 6: wire into `default_config.py` + `parse_optimizer_config.py` + dispatch tests.
- Task 7: integration smoke test (energy decreases), `slow`.
- Task 8: black + flake8(D) + mypy + full fast sweep; push.
- Final: whole-branch code review (opus), then `finishing-a-development-branch`.
